#include <iostream>

using namespace std;


int min=1000000000, max = -1000000000;

int calculate(int input_num,int add,int rem,int mul,int dev){
    if(add>0){
        
    }
    if(rem>0){

    }
    if(mul>0){

    }
    if(dev>0){

    }
}

int main(){
    int num;
    cin >> num;
    int* numlist = new int[num];
    for(int i = 0 ; i < num; i++){
        cin >> numlist[i];
    }
    int add, rem, mul, dev;
    cin >> add >> rem >> mul >> dev;

}